import WebsiteLayout from "./(website)/layout";
import WebsiteRoot from "./(website)/(pages)/(root)/page";

function Root() {
  return (
    <WebsiteLayout>
      <WebsiteRoot />
    </WebsiteLayout>
  );
}

export default Root
